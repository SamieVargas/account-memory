# Copy to .env and fill in. .env is git-ignored.

# SEC fair-access policy: every EDGAR request declares who is asking.
# Format: "Full Name email@example.com"
EDGAR_USER_AGENT=

# Answer model and Ragas judge calls (Parts 7 to 10).
ANTHROPIC_API_KEY=
