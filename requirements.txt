# Core (Parts 1 to 4). Versions pinned as each part is run.
requests>=2.31.0
pandas>=2.0.0
chromadb==1.5.9
anthropic>=1.0.0
python-dotenv>=1.0.0
pytest>=8.0.0
rank_bm25==0.2.2          # Part 5, hybrid retrieval

# Part 6 arms (bge-small, e5-small, the cross-encoder). Pulls in torch; the
# ablation reports these arms as not run when it is missing.
sentence-transformers==6.1.0

# Optional: notebooks/golden_workbench.ipynb (pip install notebook)

# Added by their parts, not yet installed or run:
# ragas                  Part 9, pinned to one version after reading its current docs
# llama-index            Part 10, the comparison arm only
# mcp                    Part 14, optional
